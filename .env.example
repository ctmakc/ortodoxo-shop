# Lead delivery (api/lead.js) — set these in Vercel → Project → Settings → Environment Variables
# Bot token from @BotFather (keep server-side only, never commit the real value)
TELEGRAM_BOT_TOKEN=
# Chat or channel id that receives the leads (e.g. -1001234567890)
TELEGRAM_CHAT_ID=
